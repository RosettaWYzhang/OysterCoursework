package com.tfl.billing;

import org.jmock.integration.junit4.JUnitRuleMockery;
import org.junit.Rule;

public class OysterCardReaderTest {
    @Rule
    public JUnitRuleMockery context = new JUnitRuleMockery();
     /*
    @Test
    public void TestOysterCardReaderTouch(){
        ScanListener scanListener = context.mock(ScanListener.class);
        OysterCardReader oysterCardReader = new OysterCardReader();
        oysterCardReader.register(scanListener);
        oysterCardReader.touch(new OysterCard());
    }
    */
}
